@page
@model TreasureHuntWebApp.Pages.ClueDoClueDont.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Clue[0].HuntID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clue[0].ClueType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clue[0].ClueText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Clue[0].Cost)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Clue) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HuntID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClueType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClueText)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

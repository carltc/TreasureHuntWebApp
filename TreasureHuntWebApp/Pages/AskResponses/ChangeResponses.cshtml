@page
@model TreasureHuntWebApp.Pages.AskResponses.ChangeResponsesModel

@{
    ViewData["Title"] = "ChangeResponses";
}

<h2>ChangeResponses</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AskResponse[0].Question)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AskResponse[0].Response)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AskResponse[0].AskDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AskResponse[0].AskType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AskResponse) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Question)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Response)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AskDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AskType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
